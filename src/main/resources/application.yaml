spring:
  application:
    name: ai-deposit-guard
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: qwen:7b
      embedding:
        options:
          model: nomic-embed-text
    chroma:
      client:
        base-url: http://localhost:8000

